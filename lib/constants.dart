import 'package:flutter/material.dart';

Color primaryColor = Color.fromARGB(255, 24, 24, 24);

const Color secondaryColor = Color.fromARGB(255, 253, 182, 35);

Color primarydarkBlack = Color.fromARGB(255, 43, 42, 42);

// Class to hold icon path and label
class _IconTextData {
  final String iconPath;
  final String label;

  const _IconTextData({required this.iconPath, required this.label});
}
